package de.renoth.dt.domain.stats;

import de.renoth.dt.domain.enums.StatType;

public class CriticalChance extends BaseStat {
    public CriticalChance(int baseValue, StatType statType) {
        super(baseValue, statType);
    }
}

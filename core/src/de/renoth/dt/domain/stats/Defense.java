package de.renoth.dt.domain.stats;

import de.renoth.dt.domain.enums.StatType;

public class Defense extends BaseStat {
    public Defense(int value, StatType statType) {
        super(value, statType);
    }
}
